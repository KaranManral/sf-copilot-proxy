# sf-copilot-proxy
A tiny local HTTP proxy that lets GitHub Copilot use the Salesforce Models API - both the Claude models (via the native Anthropic Messages API) and the Salesforce-hosted OpenAI/GPT models (via the OpenAI Chat Completions API).
